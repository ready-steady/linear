language: go

before_install:
  - sudo apt-get install gfortran

install:
  - go get -d -v ./...
  - (cd lapack && make install)
  - go build -v ./...
  - go test -v ./...

notifications:
  email:
    on_success: never
